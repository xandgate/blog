# ─── Site Tenant ───────────────────────────────────────────────────────────────
# Controls which content and config are loaded at build time.
# Leave unset (or set to "varunbaker") for varunbaker.com
# Set to "blankonpurpose" for blankonpurpose.com
# NEXT_PUBLIC_SITE_ID=varunbaker

# ─── Protected Routes ──────────────────────────────────────────────────────────
# Password for any routes listed in protectedRoutes in once-ui.config.ts
PAGE_ACCESS_PASSWORD=password

# ─── Adding a new site ─────────────────────────────────────────────────────────
# 1. Create src/resources/content.<id>.tsx
# 2. Create src/resources/once-ui.config.<id>.ts
# 3. Create src/app/blog/posts.<id>/ for that site's posts
# 4. Add the tenant check in src/resources/index.ts
# 5. Add the posts dir in src/utils/utils.ts getPostsDir()
# 6. Create a new Vercel project pointing at this repo with NEXT_PUBLIC_SITE_ID=<id>
